import sys

speed = float(input("Speed?"))
distance =  float(input("Distance?"))

print("time", distance / speed, "km", sep=" ")
#
# integer = 10
# print("The size of the integer variable is:",sys.getsizeof(integer), "bytes.")
#
# _float = 10
# print("The size of the _float variable is:",sys.getsizeof(_float), "bytes.")